import { type CommonResponse } from "../types"

const SuccessResponse : CommonResponse = {
    success : true,
    message : "SuccessFull!",
    data : {},
    error : {},
}

export default {
    SuccessResponse
}